import { Options } from './options';
declare const defaults: Options;
export declare const swCacheDefaults: {
    name: string;
    fileName: string;
    endpoint: string;
};
export default defaults;
